mimir:
  structuredConfig:
    server:
      grpc_tls_config:
        ...
    ingester_client:
      grpc_client_config:
        ...
    frontend_worker:
      grpc_client_config:
        ...
      query_scheduler_grpc_client_config: (newer beta only)
        ...
    frontend:
      grpc_client_config:
        ...
    querier:
      store_gateway_client:
        ...
    query_scheduler:
      grpc_client_config:
        ...
